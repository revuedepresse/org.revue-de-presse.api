{% extends "::base.html.twig" %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <div class="container">
        {% for email in emails %}
            <iframe width="1024" height="500" src="{{ path('weaving_the_web_dashboard_mail_show', {id: email.mailBodyId}) }}">
            </iframe>
        {% endfor %}
    <div>
{% endblock %}