{% if select_remote_form is defined %}
<h2>{{ 'remote.select_remote.title'|trans({}, 'remote') }}</h2>
<div class="row-fluid">
    {% embed 'WeavingTheWebDashboardBundle:Settings:_flash_messages.html.twig'
        with {'prefix': 'select_remote_'} %}{% endembed %}
    {{ form_start(select_remote_form) }}
    {{ form_row(select_remote_form.remotes) }}
    {{ form_end(select_remote_form) }}
 </div>
{% endif %}
