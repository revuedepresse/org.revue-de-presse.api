default:

    suites:
        default:
            contexts:
                - App\Tests\Context\ApiContext
            paths:
                - "%paths.base%/features"

    extensions:
        FriendsOfBehat\SymfonyExtension: ~
